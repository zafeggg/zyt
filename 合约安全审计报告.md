# 众赢币（ZYT）合约安全审计报告

> 审计时间：2026-08-31（第一轮） / 2026-08-31（第二轮深度检测）
> 审计范围：F:\zyt\zyt-contracts\contracts\ 全部 9 合约 + 2 接口（Solidity 0.8.24 + OpenZeppelin v5）+ 部署脚本 + 测试覆盖对照
> 审计方式：人工代码审计 + 本地链上漏洞验证（hardhat 节点 + 真实合约状态）
> 基准版本：方案 v7 冻结版对应代码
> 修复状态：第一轮 V1-V17（13 修复 + 4 保留）全部闭环；第二轮 R2-P0~P2 共 9 项修复 + 3 项决策落地（2026-09-01）
> 最终验证：44/44 单元测试全绿（第一轮 39 + 第二轮回归 5）

---

## 一、结论概览

| 级别 | 数量 | 说明 |
|---|---|---|
| 🔴 P0（资金/机制级，必须修复） | **2** | 强制卖出统计错位、claimDividend 单位混用 |
| 🟠 P1（高危，上线前必须修复） | **5** | 推荐循环、快照无权限、初始 USDT 缺口、转账滑点未实现、无限增发 |
| 🟡 P2（中危，强烈建议修复） | **6** | 到期自动销毁未实现、销毁计数反向、参数无校验、快照价脱节、LP 无凭证、分红算力不匹配 |
| 🟢 P3（低危/建议） | **4** | GST 锁后不可转、算力封顶 365 天、记账模型差异、细节 |

**修复进度（2026-08-31 第一轮）**：P0（V1/V2）+ P1（V3-V7）+ P2（V8/V9/V10/V13）+ P3（V14/V17）
**共 13 项已修复**，通过单测（39/39）+ 链上复验；V11/V12/V15/V16 共 4 项评估保留
（防闪电贷设计权衡 / 需求设计 / gas 保护 / MVP 记账模型，前端披露）；审计闭环。

**第二轮深度检测（2026-08-31，见第八章）**：新增发现 P0×2 / P1×3 / P2×4 / P3×8，
其中 9 项（R2-P0-1 / R2-P0-2 / R2-P1-1 / R2-P1-2 / R2-P1-3 / R2-P2-1 / R2-P2-2 / R2-P2-3 / R2-P2-4）
已全部修复并配套 5 项回归测试，**44/44 全绿**；P3×8 按需处理。

**其中 4 项已在链上实锤验证**（V1/V2/V3/V4），非纯理论分析。

---

## 二、🔴 P0 严重漏洞（2 项，已链上实锤）

### V1. 强制卖出统计错位：真实卖出不计入 soldAmount → 用户被误判"未卖出"被销毁

**位置**：`ZYTToken._update` + `ZYTPoolManager`（池为白名单）

**问题**：用户 `sellZyt()` 卖出时 from=用户、to=池（`isWhiteList[pool]=true`），`_update` 中
`!isWhiteList[from] && !isWhiteList[to] && !isWhiteList[msg.sender]` 因 to 为白名单而**为 false**，
→ `checkAndBurn` 不执行 → `ZYTForceSell.soldAmount` **不累计真实卖出**。
只有**用户间转账**（双方非白名单）才触发检查。

**链上验证**（✅ 实锤）：bob 已通过 `sellZyt` 卖出 34.79U（`withdrawTotal=34.79`），
但 `forceSell.soldAmount(bob) = 0`。

**影响**：用户按规则卖出到池（主要退出路径）不计"已卖量"；随后任何一次用户间转账，
`checkAndBurn` 按 `required = 余额 × 20%~60%` 计算差额 → **已实际卖出大部分的用户仍被销毁**。

**修复**：
- 方案 A：`ZYTToken._update` 转账分支改为 `if (!isWhiteList[from])` 即调用 `checkAndBurn`（to 为池也统计）；
- 方案 B：`ZYTMining.sellZyt` 成功后调用 `ZYTForceSell.recordSell(from, zytGross)` 上报卖出量（需合约新增函数）。

**✅ 修复状态（2026-08-31 已修复，采用方案 A）**：`ZYTToken._update` 判断条件改为 `!isWhiteList[from]`。
链上复验：bob 卖出 360 万 ZYT 后 `soldAmount = 3600000`（修复前为 0）。
新增单测 2 项（卖出计入 soldAmount / 池收币无强制卖出义务），全量 25/25 通过。

### V2. claimDividend 动态额度按 ZYT 数累加（单位混用残留）

**位置**：`ZYTMining.claimDividend` 第 215 行 `u.dynamicWithdrawn += share`（share 是 **ZYT 数**）

**问题**：`#3 真对账` 只修了 `claimReward` 的单位折算（`reward * price / 1e18`），
**`claimDividend` 漏修**——share 为 ZYT 数量却直接累加进 USDT 单位额度。
`emit Claimed(msg.sender, share, 0, share)` 的 usdt 参数同样传的是 ZYT 数。

**链上验证**（✅ 实锤）：alice 领取 1052.7 万 ZYT 分红 → `dynamicWithdrawn` 增加 **1052.7 万**
（等值 USDT 仅 105.27U）→ 额度 1500U **立即耗尽** → `claimReward` 全部 revert "quota exhausted"，
**用户永久停发收益**。

**影响**：分红即触发额度耗尽 → 用户无法再领产出；链下对账（真对账 #3）会再次报差异。

**修复**：`u.dynamicWithdrawn += share * pool.getTradePrice() / 1e18;` 并同步 `Claimed` 事件 usdt 参数。

**✅ 修复状态（2026-08-31 已修复）**：`claimDividend` 补折算 `usdtVal = share × getTradePrice() / 1e18`。
链上复验：alice 领取分红后额度消耗 **107.07U（USDT 等值）**（修复前错误记录 1052.7 万 U）。
真对账同步回归「6 用户一致」。新增单测 1 项，全量 25/25 通过。

---

## 三、🟠 P1 高危漏洞（5 项）

### V3. 推荐循环引用：A→B→A 祖先链含自己 → 自我奖励刷币（已链上实锤 ✅）

**位置**：`ZYTReferral.bind` 仅防 `user == ref`，不防**间接循环**

**问题**：`bind(A,B)` 后再 `bind(B,A)` 形成循环。`getAncestors` 遍历 `referrerOf` 无环检测，
A 的上级链含 A 自己。

**链上验证**（✅ 实锤）：eve→frank→eve 循环后，`getAncestors(eve)` 输出
`0x976EA7 → 0x996550 → 0x976EA7 → ...`（含自己）；eve 再入金 100U 通过"2 代奖励"自我获得
**+700.8 万 ZYT**（正常仅约 2U 等值）。

**影响**：双账户互荐即可刷取远超投入的推荐奖励（每次入金自我返利），严重稀释生态。

**修复**：`bind` 时遍历 `ref` 的祖先链，若含 `user` 则拒绝（O(depth) ≤ 20 次循环）。

**✅ 修复状态（2026-08-31 已修复）**：`bind` 增加祖先链循环检测（ref 祖先链含 user 则拒绝绑定）。
链上复验：frank→eve 循环绑定被拒（referrerOf 保持 0）、eve 祖先链不含自己、再入金 100U 仅获
理论 mint（575 万 ZYT，无自我奖励——修复前漏洞场景可刷 700 万）。新增单测 1 项。

### V4. dailySnapshot 无权限校验：任何人可传恶意 totalPower（已链上实锤 ✅）

**位置**：`ZYTDeflation.dailySnapshot(uint256 totalPower)`——无 onlyKeeper/onlyOwner

**链上验证**（✅ 实锤）：carol（非 keeper、非 owner）成功调用并把 `totalPower` 注入为 **10 亿**
（`dailyInfo[day].totalPower = 1e30`）。

**影响**：
1. **产出稀释**：恶意传超大 totalPower → 当日产出 = 10 万 × 用户算力 / 巨值 ≈ 0；
2. **DoS**：传 0 → `claimReward` 因 `totalPower==0` revert，当日产出全部无法领取；
3. **时间操纵**：抢先于 Keeper 触发快照/通缩（提前 08:00 基准），且每日一次名额被抢后 Keeper 当天无法补跑。

**修复**：Config 增加 `keeperAddress`，`dailySnapshot` 增加 `require(msg.sender == config.keeperAddress())`；
或 totalPower 做上限校验（≤ 全网真实算力上限）。

**✅ 修复状态（2026-08-31 已修复）**：ZYTConfig 新增 `keeperAddress`；`dailySnapshot` 校验
`msg.sender == keeperAddress() || msg.sender == owner()`。
链上复验：carol（非 keeper/owner）调用被拒、恶意 totalPower 未注入、owner 可正常触发（多签兜底）。
新增单测 2 项。部署脚本 keeperAddress 与 keeper 服务 KEEPER_PRIVATE_KEY 对应。

### V5. 初始底池 2.1 万 USDT 只记账面不转入 → 实际余额与账面恒差 2.1 万

**位置**：`ZYTPoolManager.initialize`——只 `transferFrom(gstToken)`，**未转入 2.1 万 USDT**

**问题**：`poolUSDT = 21000` 账面成立，但池内实际 USDT = Σ入金 − Σ卖出（不含初始 2.1 万）。
当实际余额耗尽时，账面 `require(poolUSDT >= usdtOut)` 仍通过（差 2.1 万账面），
但 `IERC20(usdt).transfer(seller, usdtOut)` 余额不足 → **revert，用户无法卖出（退出卡死）**。

**影响**：底池被抽干后全体用户无法变现（与"底池 2.1 万 U 支撑"需求不符）。

**修复**：部署流程把 2.1 万 USDT 真实转入池合约（或 `poolUSDT` 初始只按实际入池资金记账）。

**✅ 修复状态（2026-08-31 已修复）**：`initialize` 增加 `IERC20(usdt).transferFrom(msg.sender, address(this), gstAmount)`；
部署脚本/测试 fixture 增加 faucet + approve。链上验证：池实际 USDT 余额 = 账面 poolUSDT（缺口消除）。
新增单测 2 项。

### V6. 转账 10% 滑点未实现（需求未落地）

**位置**：`ZYTConfig.transferSlippage = 1000`（10%）定义但**无任何合约逻辑消费**

**问题**：需求"转账视同卖出，扣除 10% 滑点"——当前用户间转账仅触发 `checkAndBurn`（销毁检查），
**不扣 10% 滑点**。`transferSlippage` 参数形同虚设。

**影响**：转账路径零成本，可被用于绕过卖出滑点（如白名单外用户转账倒手）。

**修复**：确认需求后，在 `_update` 用户间转账分支执行 10% 滑点扣除（转入滑点池/黑洞），
或明确移除该参数。

**✅ 修复状态（2026-08-31 已修复）**：`ZYTToken` 新增 `configAddr` 引用（`setConfig`），
`_update` 用户间转账（from/to 均非白名单）按 `config.transferSlippage()` 扣 10% 并销毁（from 侧）。
新增单测 1 项（转 1000 扣 100）。

### V7. ZYT 无限增发无硬顶（总量模型矛盾）

**位置**：`ZYTToken.mintTo` 无总供应检查；`ZYTConfig.zytMaxSupply`（21 亿）未被任何逻辑使用

**影响**：入金 mint + 每日产出 10 万 + 推荐奖励 + 分红均无上限增发 → 总供应持续超 21 亿。
若"21 亿总量"是硬约束则违背需求；若为增发挖矿设计需明确并加运营 cap。

**修复**：`mintTo` 校验 `totalSupply() + amount <= config.zytMaxSupply()`（或按需求确认增发模型）。

**✅ 修复状态（2026-08-31 已修复，保险丝方案）**：ZYTConfig 新增 `totalSupplyCap`（默认 1000 亿 =
初始池 21 亿 + 增发空间，多签可调）；`ZYTToken.mintTo` 校验 `totalSupply() + amount <= totalSupplyCap()`。
语义说明：21 亿为**初始底池量**（需求原文），增发是产品设计；cap 为防失控保险丝。
新增单测 1 项（超 cap 入金 revert 且资金回滚）。

---

## 四、🟡 P2 中危漏洞（6 项）

| # | 位置 | 问题 | 影响 | 修复建议 | 状态 |
|---|---|---|---|---|---|
| V8 | ZYTForceSell | **到期"自动销毁"未实现**：checkAndBurn 仅在用户间转账 hook 触发，用户不转账则永不销毁 | 与需求"未执行自动销毁"不符；机制实际只惩罚转账 | sellZyt 内联到期检查 / keeper 调 forceSell 结算函数 | ✅ 已修复（settleExpired + keeper 地址） |
| V9 | ZYTForceSell.checkAndBurn | 销毁后 `soldAmount -= burned`（进度反向扣减） | 进度永远不足 → 反复销毁（虽按余额动态收敛，语义混乱，链下难对齐） | 销毁不扣 soldAmount（或改为进度 += 销毁额） | ✅ 已修复（删除扣减行） |
| V10 | ZYTConfig.setUint/setAddress | **无取值范围校验** | 多签可将滑点/费率/门槛设为任意值（含 0、>100%），极端参数使 `netZyt` 溢出 revert | 每参数加合理上下限 require | ✅ 已修复（费率类 ≤10000） |
| V11 | getTradePrice 快照价 | 快照价 vs 实时价脱节：大额买入后实时价远高于快照价，用户按快照价卖出低价抽血 | 价格滞后、池不平衡（v7 防闪电贷权衡的代价） | 接受 + 前端披露；或加"价格偏离上限"暂停保护 | ⚪ 保留（v7 防闪电贷权衡，前端披露） |
| V12 | addLiquidity | 转入 USDT 无 LP 凭证/取回权，成为公共流动性被卖出者抽走 | 阶段 2 用户资金风险（需求如此，需披露） | 前端显著提示；或发 LP 凭证 | ⚪ 保留（需求设计，前端披露） |
| V13 | claimDividend | 分红份额按"最近 30 天首日全网算力"计算，时间不匹配；无领取记录 | keeper 停更后占比虚高，可超额领取（池空为止） | 按当日快照算力 + 加 claim 记录 | ✅ 已修复（lastDividendDay 按日限次） |

---

## 五、🟢 P3 低危/建议（4 项）

| # | 位置 | 问题 | 状态 |
|---|---|---|---|
| V14 | GSTToken.lockRemaining | 锁定后 `transfer` 仅限黑洞/0 地址 → 未来接 DEX 时池内 GST 无法转出 | ✅ 已修复（transferAllowed 豁免白名单） |
| V15 | ZYTCompute.powerWithCompound | 算力复利封顶 365 天，超期停止复利 | ⚪ 保留（gas 保护设计，披露） |
| V16 | settleSell poolGST | 滑点档位按**账面** poolGST 判定，实际 GST 从未减少 | ⚪ 保留（MVP 记账模型，接 DEX 时重构） |
| V17 | 事件/一致性 | `Sold` 事件不包含滑点明细 | ✅ 已修复（Sold 加 rate 参数，keeper 三处同步） |

---

## 六、已确认安全项（非漏洞，供参考）

| 项 | 结论 |
|---|---|
| 重入防护 | ✅ deposit/sellZyt/claimReward/claimDividend 均 `nonReentrant`；USDT/ZYT 为无回调标准 ERC20 |
| 买入白名单（v7） | ✅ deposit 前置校验，防闪电贷第一道防线 |
| 当日快照基准价（v7） | ✅ 买卖统一 `getTradePrice`，闪电贷无法单笔操纵价格 |
| 权限模型 | ✅ mint/burn/结算均限 onlyMinter/onlyMining/onlyDeflation/onlyOwner |
| 整数溢出 | ✅ Solidity 0.8.24 内建检查（除 V10 参数失控场景） |
| 出局/额度/复利计算 | ✅ 纯函数库 ZYTCompute，边界（365 天封顶、0 值）已处理 |

---

## 七、修复优先级建议

**第一批（P0，立即修复）**：
1. V1 强制卖出统计错位（方案 A 或 B）
2. V2 claimDividend 单位折算

**第二批（P1，上线前必做）**：
3. V3 推荐循环防护
4. V4 dailySnapshot keeper 权限
5. V5 初始 USDT 转入
6. V6 转账滑点（确认需求后实现或移除）
7. V7 增发上限

**第三批（P2，随版本迭代）**：V8-V13

**第四批（P3，低成本顺手做）**：V14-V17

---

## 八、第二轮深度检测与修复（2026-08-31）

> 复核范围：9 合约全量源码 × 部署脚本（deploy.js 生产版 / deploy-plain.js 本地版）× 39 项单测覆盖对照。
> 结论：第一轮 V1-V17 修复均正确落位、无回退；本轮新增 2 P0 / 3 P1 / 4 P2 / 8 P3，9 项已全修复（含 3 项决策落地），44/44 单测全绿。

### 8.1 🔴 P0（必须修复才能上线）

| # | 位置 | 问题 | 影响 | 状态 |
|---|---|---|---|---|
| R2-P0-1 | `deploy.js`（生产脚本） | **漏设 `zyt.setConfig(configAddr)`**（deploy-plain.js 有） | configAddr=0 → V6 转账 10% 滑点税跳过 + V7 totalSupplyCap 保险丝失效，两项安全机制生产静默失效 | ✅ 已修复（补 setConfig，与本地脚本对齐） |
| R2-P0-2 | `deploy.js` + `getStage()` | **未设 poolStage1/2USDT → 部署后永远 stage1 → 系统锁死** | 初始 poolUSDT=2.1 万 < 1000 万 → deposit「buy disabled in stage1」、addLiquidity「not stage2」；而 poolUSDT 只能靠入金增长 → **永远无法启动**。本质是「阶段1只卖不买」与「无 mint 通道」的方案矛盾 | ✅ 已修复（决策 17：poolStage1USDT=0，初始 stage2 启动） |

### 8.2 🟠 P1（合约内资金逻辑）

| # | 位置 | 问题 | 影响 | 状态 |
|---|---|---|---|---|
| R2-P1-1 | `ZYTMining.claimReward` | **无 `day >= u.powerDay` 校验** | 新用户可回溯领取入金前所有历史日产出（`_powerOf` 对 day<powerDay 返回 base）→ 一次性稀释早期用户 | ✅ 已修复（加 require，revert「Mining: before deposit」+ 回归测试） |
| R2-P1-2 | `ZYTMining.claimDividend` | **缺 `isQuotaExhausted` 检查**（claimReward 有） | 动态额度耗尽后仍可每日领分红，绕过「耗尽停发」设计 | ✅ 已修复（与 claimReward 对齐 + 回归测试） |
| R2-P1-3 | `ZYTMining.deposit` + claim | **静态出局（isExited）后复投不重置** | 出局用户复投后 claim 全被拒 → 复投资金只拿到 mint 的 ZYT、算力收益永锁 | ✅ 已修复（决策 18：deposit 重置 isExited，withdrawTotal 保留，2 倍累计延续 + 回归测试） |

### 8.3 🟡 P2（边界 / 一致性）

| # | 位置 | 问题 | 影响 | 状态 |
|---|---|---|---|---|
| R2-P2-1 | `claimReward` | **`_rewardClaimed` 标记先于 mintTo** | mintTo 因 totalSupplyCap revert 时标记已置位 → 当日产出永久丢失无法重试 | ✅ 已修复（置位 + 额度累加后移到 mint 成功后 + 回归测试） |
| R2-P2-2 | `payoutDividend` | 分红走 transfer（from=池白名单）→ **不触发 onMint，分红 ZYT 不受 60 天强制卖出约束**；与 claimReward（mint）渠道不一致 | 分红可囤积绕过强制卖出 | ✅ 已修复（决策 19：`_update` 对 from 白名单→to 非白名单补 onMint 初始化；MARKET/TECHNICAL 白名单豁免 + 回归测试） |
| R2-P2-3 | `deploy.js` | **漏设 keeperAddress** | owner 转多签后 dailySnapshot 无人可触发（快照链路断裂） | ✅ 已修复（KEEPER_ADDRESS 环境变量，默认 deployer 兜底） |
| R2-P2-4 | `deploy.js` | **白名单为空 + buyWhitelistEnabled=true → 无人能入金** | 部署后需运营手动逐个加白名单，易漏 | ✅ 已修复（WHITELIST 环境变量批量放行，空则后续多签添加） |

### 8.4 ⚪ P3（低危 / 运维，按需处理）

| # | 位置 | 问题 |
|---|---|---|
| R2-P3-1 | 用户间转账 | 全转账需留足 10% 税 + 强制销毁差额，否则 revert（体验边界，设计使然） |
| R2-P3-2 | `addLiquidity` | USDT 永锁池中无赎回机制（LP 资金风险，需披露确认） |
| R2-P3-3 | `pause` | 不冻结 claimReward/claimDividend（暂停时允许提收益，确认是否有意） |
| R2-P3-4 | `config.snapshotTime` | 参数从未被读取，dailySnapshot 不校验 08:00，靠 keeper 调度 |
| R2-P3-5 | `setUint` | 无参数关系校验（max≥min、stage2≥stage1 等） |
| R2-P3-6 | `ZYTReferral.downlineCount` | 死代码；bind 环检测仅 20 代（>20 代环无分红影响） |
| R2-P3-7 | `dailyBurn` | 真实余额不足时 `_burnPoolZyt` 截断，poolZYT 账面可能虚低（极端） |
| R2-P3-8 | keeper 漏快照 | `_latestTotalPower` 回退 30 天旧值，分红分配失真（Keeper 必须每日跑） |

### 8.5 测试盲区（第二轮补 5 项回归后仍留）

- ✅ 已补：回溯领取被拒 / 额度耗尽后 claimDividend 被拒 / 同日二次领取被拒 / 复投重置可领产出 / 营销收款不初始化
- ⬜ 遗留：**阶段 1/2 真实启动路径**（现用例全在 stage3 下运行）；**生产 deploy.js 接线完整性断言**（建议加 post-deploy-check：configAddr / keeperAddress / 白名单 / poolStage1USDT=0）

> ⚠️ 本报告为内部技术审计（代码审查 + 链上验证），**不替代第三方专业审计**
> （CertiK / SlowMist / Beosin）。上线前仍建议完成第三方审计 + bscscan verify + GoPlus 自检。
