# 软件产品上线流程清单（v2.0 可执行操作版）

> 定位：每个阶段、每一步给出**具体怎么做**——命令、判定标准、产出物。通用步骤可直接套用；【ZYT】为项目特化操作。
> 使用：打印本表按顺序执行；每步"产出物"为完成凭证，由复核人确认签字。
> 原则：双人操作、每步验证、不在计划内不改动、任何时刻可回滚。

---

## 阶段 0：上线准入与冻结（T-7 天）

### 0.1 确定上线范围
- **做什么**：锁定本次上线内容，防止范围蔓延。
- **怎么做**：
  1. 列出变更清单：功能新增 / bug 修复 / 配置变更 / 依赖升级，逐项编号。
  2. 逐项标注影响面（前端/后端/数据/合约）与风险等级（高/中/低）。
  3. 开会逐项确认"进/不进"，产出《上线范围确认表》。
- **判定**：范围外需求统一打回下版本；高危项必须附风险应对。
- **产出**：《上线范围确认表》。

### 0.2 代码冻结
- **做什么**：冻结开发分支，只允许 P0 修复进入 release。
- **怎么做**：
  1. `git checkout develop && git pull`
  2. `git checkout -b release/vX.Y.Z`（从 develop 创建）
  3. 在 Git 平台开启 release 分支保护：禁止直接 push，仅 PR 合入；PR 需 2 人批准。
  4. 之后所有变更走"变更审批"：填《上线变更单》，P0 修复才可合入。
- **判定**：release 分支相对 baseline 的 diff 列表与范围清单一致。
- **产出**：release/vX.Y.Z 分支 + 变更审批记录。

### 0.3 版本规划
- **做什么**：确定版本号与可回滚基准。
- **怎么做**：
  1. 定版本号（语义化：主.次.修复）。
  2. 写 CHANGELOG（新功能 / 修复 / 破坏性变更，引用 issue 号）。
  3. 打 tag：`git tag vX.Y.Z && git push origin vX.Y.Z`（上线成功后）。
- **判定**：回滚基准 = 当前生产 tag（上线前确认其构建产物可恢复）。
- **产出**：CHANGELOG + 版本 tag。

### 0.4 确定上线窗口
- **做什么**：选业务低峰期，避开关键时段。
- **怎么做**：查看业务流量曲线（日活时段），选低峰；【ZYT】避开每日 08:00-08:10 快照/通缩窗口，不在通缩日 08:00 前后 2h 内动 keeper。
- **判定**：窗口内操作时长 ≤ 预留缓冲（如 2h 窗口留 3h 缓冲）。
- **产出**：上线时间表（见文末模板）。

### 0.5 组织与通讯
- **做什么**：明确角色，建立指挥链。
- **怎么做**：
  1. 填角色表：决策人（1 名，最终拍板）/ 执行人（1-2 名）/ 复核人（1-2 名，与执行人不同）/ 观察员。
  2. 建上线通讯群，群内仅同步操作记录与决策，不闲聊。
  3. 定义中止条件并群内宣告：任何人发现异常可喊"中止"，决策人 5 分钟内拍板继续/回滚。
- **判定**：角色表所有人确认到位；中止条件已宣告。
- **产出**：角色表 + 通讯群。

### 0.6 Go/No-Go 评审
- **做什么**：正式放行。
- **怎么做**：决策人主持评审，逐项过阶段 0-1 清单，任何"否"→ No-Go，回到对应阶段整改后重评。
- **一票否决项**：未闭环的高危安全漏洞；数据迁移未演练；回滚基准不可用；【ZYT】审计未闭环 / 参数核对表未签字。
- **产出**：评审记录（决策人签字）。

---

## 阶段 1：上线前准备（T-5 至 T-1 天）

### 1.1 代码合并与构建

**1.1.1 合并至 develop**
- **怎么做**：
  1. `git checkout develop && git pull origin develop`
  2. `git merge --no-ff feature/xxx`（逐个功能分支，解决冲突）
  3. `git push origin develop`，触发 CI。
- **判定**：CI 全绿（构建 + 测试 + 静态检查）。
- **产出**：develop 合并记录 + CI 绿标。

**1.1.2 创建 release 分支并评审**
- **怎么做**：
  1. `git checkout -b release/vX.Y.Z origin/develop && git push`
  2. 核心代码走 PR 评审：至少 1 名高级成员 approve；**资金/密钥/权限相关模块双人评审**（approve 2 人）。
- **判定**：PR 全部 approved；无未关闭的"阻塞"评论。
- **产出**：release 分支 + PR 评审记录。

**1.1.3 生产构建**
- **怎么做**：
  1. CI 从 release 分支构建（或本地构建）：前端 `npx vite build`；后端 `npm run build`。
  2. 记录产物指纹：`sha256sum dist/*` 输出归档。
  3. 产物目录留档（命名 `dist-vX.Y.Z-YYYYMMDD`，保留最近 N 版供回滚）。
- **判定**：构建产物目录内容完整（html/js/css/资源），指纹已记录。
- **产出**：构建产物包 + 指纹清单。

**【ZYT】特化**：
- 合约源码冻结：不再改动 .sol；上线前最后跑一次 `npx hardhat run scripts/probe-bytecode.js` 确认线上=本地字节码（Mining 库链接占位符差异属正常）。
- bscscan 验证全绿（已 9/9）：`cd zyt-contracts && http_proxy=http://127.0.0.1:7890 HTTP_PROXY=… https_proxy=… HTTPS_PROXY=… npx hardhat run scripts/verify-all-testnet.js --network bscTestnet`（主网部署后需用 `--network bsc` 对主网地址再验一遍）。
- 前端构建后：index.html 资源引用更新 `?v=YYYYMMDDxx` 缓存参数（每次上传必须递增，防浏览器旧缓存）。

### 1.2 测试验收

**1.2.1 自动化测试**
- **怎么做**：
  1. 单测：`npx hardhat test`（合约）或 `npm test`（后端），覆盖率达标。
  2. 回归：跑全量测试套件，重点本次变更点。
- **判定**：全部通过；失败用例需修复后全量重跑，禁止跳过。

**1.2.2 冒烟测试**
- **怎么做**：【ZYT】testnet 干净链冒烟：`USDT_MOCK=1 npx hardhat run scripts/smoke-testnet.js --network bscTestnet`，P0-P6 45 断言全绿。
- **判定**：冒烟脚本 exit 0；关键数值与预期一致（入金/快照/分红/卖出/转账税）。
- **产出**：冒烟报告（保存输出日志）。

**1.2.3 UAT 业务验收**
- **怎么做**：产品/运营按《验收用例清单》在预发布环境逐项操作（真实钱包、真实链路），记录通过与问题。
- **判定**：阻塞性问题 0；非阻塞问题有规避方案或已排期。
- **产出**：《UAT 验收单》签字。

**1.2.4 性能压测**
- **怎么做**：
  1. 压测工具（wrk/ab/k6）打核心接口：`wrk -t8 -c100 -d60s http://<host>/api/xxx`。
  2. 记录 QPS、P95 延迟、错误率。
- **判定**：QPS 达目标；P95 在 SLA 内；错误率 < 1%；压测后无内存/连接泄漏（观察 10 分钟指标回稳）。
- **产出**：压测报告。

**1.2.5 安全扫描**
- **怎么做**：`npm audit --production`（依赖漏洞）；渗透测试（重点：越权/注入/敏感信息）；【ZYT】GoPlus 安全检测（合约/代币）。
- **判定**：高危漏洞 0；中危有修复计划；GoPlus 检测项全绿。
- **产出**：安全扫描报告。

### 1.3 环境配置

**1.3.1 服务器与网络**
- **怎么做**：
  1. 确认服务器资源（CPU/内存/磁盘）、OS 版本、时区（业务时区）【ZYT keeper 本地 Windows 时区 Asia/Shanghai，cron 已显式 tz=UTC 不依赖系统时区】。
  2. 安全组/防火墙：仅开放 80/443（+ 必要端口）；管理端口（22 等）限定来源 IP。
  3. SSL 证书签发与续期（certbot 或云证书），确认有效期 > 30 天。
- **判定**：`curl -I https://<domain>` 返回 200；证书有效期符合。
- **产出**：环境就绪确认单。

**1.3.2 环境变量核对**
- **怎么做**：
  1. 维护《env 核对表》：变量名 / 值来源 / 是否敏感 / 生产值。
  2. 逐行核对生产 .env 与核对表一致；敏感项（私钥/密码）确认不打印、不入日志、不入代码库（`.gitignore` 已排除）。
  3. 【ZYT keeper】本地 `.env`（含 KEEPER_PRIVATE_KEY）已在 .gitignore；核对 CHAIN_ID=97、RPC、6 合约地址、START_BLOCK、DB_URL。
- **判定**：核对表逐行签字；`grep -r "PRIVATE_KEY\|password" . --include="*.md"` 无泄漏。
- **产出**：env 核对表签字版。

**1.3.3 密钥与权限**
- **怎么做**：
  1. 生产密钥单独管理（密码管理器/密钥服务），不随代码分发。
  2. 最小权限：DB 用户仅授必要库权限（非 root）。
  3. 【ZYT】keeper 签名钱包与资金钱包完全隔离：`KEEPER_PRIVATE_KEY` 地址在 ZYTConfig `keeperAddress` 注册，仅能触发 dailySnapshot，无任何资金权限。
- **判定**：权限矩阵核对；无 root/超管在生产运行。
- **产出**：权限矩阵。

**1.3.4 预发布演练**
- **怎么做**：在 staging（与生产同构）完整走一遍部署步骤（阶段 2 的 1-5 步），记录实际耗时与报错。
- **判定**：演练全通，耗时在窗口预算内；发现的问题已修复并复演。
- **产出**：演练记录。

**【ZYT】特化**：
- 合约参数核对表：9 合约地址、owner、keeperAddress、买入白名单、deflationFloor、滑点档位、poolStage1/2USDT 逐项与部署记录比对（链上 `eth_call` 读取 vs 部署日志）。
- 多签就绪：owner 权限移交 Gnosis Safe（营销+技术多签）；移交交易在测试网先演练一遍签名流程；**移交后单一 EOA 不再有管理权限**。
- 前端配置核对：主网 chainId(56)、RPC、合约地址、API 域名、`apiBase` 降级开关。
- 【ZYT 托管待定】ZYT 前端生产托管方案未定（独立域名/CDN，不使用 GYT 服务器）——上线前必须确定并完成 1.3.1/1.3.4。

### 1.4 数据备份与恢复演练

**1.4.1 数据库备份**
- **怎么做**：
  1. 全量备份：`mysqldump -u<user> -p <db> > backup-$(date +%F).sql`（含 `--single-transaction` 避免锁表）。
  2. 增量：开启 binlog（`log-bin`）并定期归档。
  3. 备份文件**异地/异介质**存储（对象存储/另一台机器）。
  4. 【ZYT keeper】`zyt_keeper` 库：events/users/snapshots/pool_state/reconcile_results/force_sell 全表导出。
- **判定**：备份文件大小合理、可打开；备份任务写入定时计划。
- **产出**：备份文件 + 备份任务配置。

**1.4.2 恢复演练**
- **怎么做**：在隔离环境执行恢复：`mysql -u<user> -p <db> < backup.sql` → 启动应用验证数据完整性（表行数、关键业务数据）。
- **判定**：恢复成功；恢复耗时记录（作为 RTO 参考）。
- **产出**：恢复演练记录。

**【ZYT】特化**：
- 合约部署产物冷备：abi 文件、deployedBytecode、构造参数、部署交易哈希、部署私钥（**离线介质**，多份）。
- 上线前资金链上快照：底池/多签/营销地址余额截图 + 链上查询记录存档。

---

## 阶段 2：上线执行（T-0）

> 按序执行，**每步验证通过才进下一步**；执行人操作、复核人核对输出。

| 序 | 做什么 | 怎么做 | 验证点（怎么做） |
|---|---|---|---|
| T-30min | 集合对齐 | 群内过 checklist；确认回滚基准产物路径；宣告中止条件 | 全员已读；基准产物可访问 |
| 1 | 数据层变更（如有） | ①再次备份 `mysqldump`；②执行迁移脚本 `node scripts/migrate.js`；③验证 | 迁移日志无 ERROR；`SHOW TABLES` 新表存在；旧代码冒烟兼容 |
| 2 | 后端发布 | ①`pm2 deploy` 或上传新版本目录；②`pm2 restart <app>`；③健康检查 | `curl http://127.0.0.1:<port>/health` = ok；日志无堆栈 |
| 3 | 链下服务【ZYT keeper】 | ①`cd F:/zyt/zyt-keeper`；②先停旧实例（确认无残留占用 8080：`netstat -ano \| grep :8080`）；③`node src/index.js`（后台）；④看启动日志 | 日志出现：`ledger rebuild users=N`、`keeper start cron="0 0 * * *" tz=UTC`、`api start :8080`、`reconcile ok 对账 N 用户一致`；`curl http://127.0.0.1:8080/health` = ok |
| 4 | 前端发布 | ①上传 dist 到托管位置（`?v=` 参数已递增）；②CDN/缓存刷新 | 浏览器强制刷新（Ctrl+F5）后版本号/指纹正确；资源 200 |
| 5 | DNS/流量切换（如涉及） | 修改解析/负载均衡权重，按 10%→50%→100% 灰度 | `dig`/`nslookup` 解析生效；各比例下错误率正常 |

**执行纪律**：
- 计划外变更一律拒绝；每步留存命令与输出截图。
- 【ZYT 合约】合约部署（主网）是**不可回滚**动作：安排在 T-1 前完成并验证（部署 → 字节码核对 → 参数核对 → 白名单放行），T-0 只做应用层；主网部署出问题走阶段 5 的"暂停+迁移"预案，不在现场修补。

---

## 阶段 3：上线后验证（T+0 至 T+2h）

### 3.1 核心链路冒烟
- **怎么做**：在生产环境用真实用户路径走一遍：
  通用：注册/登录/主功能/支付。
  【ZYT】钱包连接 → USDT 授权 → 入金 → 算力/产出领取 → 分红领取 → 卖出（滑点档位显示）→ 转账（10% 税）→ 强制卖出窗口展示。
- **判定**：每步成功且数值与预期一致（金额、余额、事件记录）。
- **产出**：冒烟执行记录。

### 3.2 数据一致性核对
- **怎么做**：【ZYT】三方一致：
  1. 链上：读 pool 合约 `poolUSDT/poolZYT/snapshotPrice`（eth_call）。
  2. keeper：`curl http://127.0.0.1:8080/stats` 的 pool_state。
  3. 前端展示值。
  三者比对；再触发一次 `reconcile`（`/reconcile` 需 admin token）确认 diff=0。
- **判定**：三方数值一致；reconcile diff=0；users 表与链上 userList 长度一致。
- **产出**：一致性核对单。

### 3.3 性能与错误率
- **怎么做**：观察监控面板：API 错误率、P95 延迟、5xx 数量、慢查询；与上线前基线对比。
- **判定**：错误率 < 1% 且无上升趋势；P95 在 SLA 内；无新增 5xx 堆栈。
- **产出**：基线对比截图。

### 3.4 第三方与反馈通道
- **怎么做**：确认 RPC 节点、CDN、外部服务正常；开启客服/公告渠道。
- **判定**：第三方连通测试通过；反馈渠道有专人值守。
- **产出**：发布公告（变更内容/已知问题/反馈入口）。

---

## 阶段 4：监控要点（上线后 72h 加密巡检，之后转常态）

### 4.1 监控配置
- **怎么做**：
  1. 基础设施：CPU/内存/磁盘/带宽面板；进程存活（pm2 status / Windows 任务计划）。
  2. 应用：错误日志聚合 + 错误率告警；慢查询告警。
  3. 业务：【ZYT】入金量/卖出量/算力总量/用户增长。
  4. 链下【ZYT keeper】5 规则（R1 底池突变 / R2 大额卖出 / R3 滑点跳变 / R4 索引延迟 / R5 失败率）——检查 monitor 启动日志 `rules=R1/R2/R3/R4/R5`；告警 webhook 已实测。
  5. 链上【ZYT】：每日快照成功、对账 diff、异常事件、多签操作。
- **判定**：所有告警通道实测可达（上线前发过测试告警）；规则参数合理（冷却时间防风暴）。

### 4.2 每日巡检（固化动作）
- **怎么做**（每天固定时间执行并记录）：
  1. 【ZYT】查 keeper 日志确认今日快照成功：`SELECT * FROM snapshots ORDER BY day DESC LIMIT 3`（MySQL）应含当日 day。
  2. 【ZYT】对账：keeper 启动日志/reconcile_results 表 status=ok、diff_users=0。
  3. 进程存活、磁盘水位、备份任务执行情况。
  4. 错误日志扫描（过去 24h 新增 ERROR 级）。
- **判定**：以上全绿；异常当天处理并记录。
- **产出**：《每日巡检记录表》。

### 4.3 告警响应
- **分级**：P0 立即电话（决策人）；P1 15 分钟响应；P2 工作时间。
- **怎么做**：告警触发 → 确认人接单 → 判断级别 → 按预案处理 → 记录关闭。
- **判定**：SLA 内响应；升级链路（值班 → 负责人 → 决策人）已建立。

---

## 阶段 5：回滚预案

### 5.1 触发判定
| 级别 | 判定标准 | 动作 |
|---|---|---|
| P0 | 核心功能不可用 / 资金或数据安全风险 / 错误率持续 > 5% / 【ZYT】快照连续 2 日未触发 | **立即回滚**，决策时限 10 分钟 |
| P1 | 非核心功能异常 / 性能明显劣化 | 优先 hotfix 或降级开关 |
| P2 | 不影响主流程的体验问题 | 记录随下版本修复 |

### 5.2 分层回滚步骤（具体怎么做）

**前端回滚（分钟级）**
1. 从产物归档取上一版 dist（`dist-vX.Y.Z-1`）。
2. 上传覆盖 + `?v=` 参数回退为上一版编号。
3. CDN/缓存刷新。
4. 验证：强制刷新页面加载旧版本号资源 200。

**后端回滚**
1. 部署目录保留最近 N 版；`pm2 startOrReload ecosystem.<prev>.config.js` 或切换软链。
2. 健康检查通过后确认。
3. 前提：DB schema 向前兼容（新表不删旧列、新列有默认值）。

**数据库**
- 原则：**只回代码不回数据**。schema 变更遵循可逆设计（只加列不删列）。
- 极端情况（数据损坏）：用备份恢复 + 补偿脚本（binlog 回放到事故点）。

**配置回滚**：.env 回退上一版备份 → 重启服务 → 验证。

**【ZYT keeper】回滚**
1. 停当前实例（释放 8080）。
2. 恢复上一版本代码（git checkout 上一 tag）。
3. 启动并按需将 START_BLOCK 对齐（若停机超 prune 窗口，查最新块并更新 .env）。
4. 验证 indexer/ledger/reconcile 全绿。

**【ZYT 合约】不可升级合约——回滚预案链**
1. **止血**：如合约具备 pause 能力，多签触发暂停（限制入金/交易）。
2. **评估**：确认问题范围（功能缺陷 / 资金风险 / 参数错误）。
3. **迁移**：部署新合约（修复版）→ 前端/keeper 切换地址 → 按链上状态迁移资金与用户余额（底池、用户额度、待领分红逐项核对）。
4. **公告**：向用户说明迁移方案与时间表。
5. 可配置项（keeperAddress、白名单、滑点档位、阶段阈值）优先用多签调整，避免合约级迁移。

### 5.3 回滚后行动
- **怎么做**：48h 内复盘（根因/时间线/决策点/改进项，产出《复盘报告》）；hotfix 走完整流程重新上线；上线前至少演练一次前端+后端回滚，此后每季度一次。
- **判定**：复盘报告完成；改进项进下版本 backlog；演练记录存档。

---

## 附 A：上线当日时间表模板

| 时间 | 做什么 | 怎么做（命令/动作） | 执行人 | 复核人 | 结果 |
|---|---|---|---|---|---|
| T-30min | 集合对齐 | 群内 checklist、基准确认、宣告中止条件 | | | |
| T-20min | 最终备份 | `mysqldump ...`（DB）＋ 配置/产物归档确认 | | | |
| T-10min | 监控基线 | 面板截图（错误率/延迟/容量）存档 | | | |
| T-0 | 阶段 2 步骤 1-5 | 按表执行、每步验证 | | | |
| T+30min | 核心冒烟 | 阶段 3.1 清单逐项 | | | |
| T+1h | 一致性核对 | 阶段 3.2 三方比对 + reconcile | | | |
| T+2h | 上线确认/回滚决策点 | 决策人签字"继续/回滚" | | | |
| T+24h | 复盘会 | 产出复盘报告 | | | |

## 附 B：关键命令速查

```bash
# 构建与测试
npx vite build                                  # 前端生产构建
npx hardhat test                                # 合约单测
USDT_MOCK=1 npx hardhat run scripts/smoke-testnet.js --network bscTestnet   # ZYT testnet 冒烟
http_proxy=http://127.0.0.1:7890 HTTP_PROXY=http://127.0.0.1:7890 https_proxy=http://127.0.0.1:7890 HTTPS_PROXY=http://127.0.0.1:7890 npx hardhat run scripts/verify-all-testnet.js --network bscTestnet  # bscscan 验证

# 备份与恢复
mysqldump --single-transaction -u<user> -p <db> > backup-$(date +%F).sql
mysql -u<user> -p <db> < backup.sql             # 恢复

# keeper（本地）启动与健康
cd F:/zyt/zyt-keeper && node src/index.js       # 启动（后台）
curl http://127.0.0.1:8080/health               # 健康检查，期望 {"ok":true}
curl http://127.0.0.1:8080/stats                # 池状态 + 最近快照
netstat -ano | grep ":8080"                     # 端口占用排查（启动前确认无残留）

# 进程与端口
pm2 list / pm2 restart <app>                    # 后端进程管理
```

> 文档版本：v2.0（2026-09-02）｜ 关联：众赢币(ZYT)_技术方案.md §8 / 上线流程清单 v1.0
