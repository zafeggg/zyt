# 软件产品上线流程清单（通用 SOP + ZYT 特化项）

> 适用范围：Web 应用 / DApp / 链下服务的生产发布。
> 使用方式：每个阶段按清单逐项勾选；【ZYT】标注为本项目特有检查项，其余为通用项。
> 原则：**双人操作、每步验证、不在计划内不改动、任何时刻可回滚**。

---

## 阶段 0：上线准入与冻结（T-7 天）

| # | 关键步骤 | 注意事项 |
|---|---|---|
| 0.1 | 确定上线范围清单（功能/修复/配置变更）与非目标 | 范围外需求一律进下版本 |
| 0.2 | 代码冻结：关闭合并窗口，仅允许 P0 修复进 release 分支 | 冻结后变更需重新验收 |
| 0.3 | 版本规划：版本号、Git tag、CHANGELOG | 明确回滚基准 = 当前生产版本 |
| 0.4 | 确定上线窗口：业务低峰期 | 【ZYT】避开每日 08:00-08:10 快照/通缩窗口 |
| 0.5 | 组织准备：上线决策人、执行人、复核人（双人机制）、通讯群 | 群内同步操作记录与截图 |
| 0.6 | Go/No-Go 评审会：准入 checklist 全绿方可执行 | 一票否决项：未闭环的高危问题 |

---

## 阶段 1：上线前准备（T-5 至 T-1 天）

### 1.1 代码合并与构建

- [ ] feature 分支全量合并至 develop，CI 全绿
- [ ] 创建 release/vX.Y.Z 分支，此后只进修复
- [ ] 代码评审：至少 1 名高级成员批准；资金/密钥相关模块**双人评审**
- [ ] 生产构建：CI 从 release 分支构建，产物带版本指纹；依赖版本锁定（lock 文件）
- [ ] 构建产物归档（可复现构建）
- 【ZYT 合约】源码冻结不再改动；`deployedBytecode` 与线上字节码一致性核对（库链接占位符差异属正常）；bscscan 源码验证全绿（9 合约+库 `Successfully verified`）
- 【ZYT 前端】`npx vite build` 刷新 dist；index.html 资源引用更新 `?v=YYYYMMDDxx` 缓存参数

### 1.2 测试验收

- [ ] 单元测试全绿，覆盖率达标
- [ ] 集成/回归测试全绿（核心链路 + 本次变更点）
- [ ] 冒烟测试在预发布环境通过【ZYT：smoke-testnet 45/45 断言】
- [ ] UAT 业务验收：产品/运营签字确认
- [ ] 性能压测：核心接口容量、瓶颈、限流阈值确认
- [ ] 安全扫描：渗透测试、依赖漏洞扫描（npm audit / GoPlus）
- 【ZYT 合约】第三方审计报告全部闭环（高危/中危修复逐项确认）；测试网试运行 2-4 周无 P0/P1
- 【ZYT】上线参数核对表逐项签字（见 1.3）

### 1.3 环境配置

- [ ] 服务器/带宽/域名/SSL 证书就绪（证书有效期 > 30 天）
- [ ] 环境变量核对表逐项核对（来源、敏感项脱敏检查）
- [ ] 密钥管理：生产密钥不入代码库；.gitignore 确认；最小权限
- [ ] 防火墙/安全组：仅开放必要端口；管理端口不暴露公网
- [ ] 预发布环境与生产同构（仅连接串不同），完整走一遍部署流程
- 【ZYT】合约配置核对表：9 合约地址、owner、keeperAddress、买入白名单、通缩底线、滑点档位、阶段阈值逐项与部署记录比对
- 【ZYT】多签就绪：owner 权限移交 Gnosis Safe（营销+技术多签），单一 EOA 不持有管理权限；多签签名流程演练一次
- 【ZYT keeper】签名钱包（KEEPER_PRIVATE_KEY）与资金完全隔离，仅作定时触发器；KEEPER_ADDRESS 已在 ZYTConfig 注册
- 【ZYT 前端】主网 chainId(56)、RPC、合约地址、API 域名配置核对；钱包连接（MetaMask/TP 等）主网链校验通过
- 【ZYT keeper】告警通道实测：向 ALERT_WEBHOOK_URL 发一条测试告警确认可达

### 1.4 数据备份与恢复演练

- [ ] 数据库全量备份，并在隔离环境**实际恢复一次**验证可用
- [ ] 备份策略：全量周期 + binlog 增量；备份异地/异介质存储
- [ ] 关键配置、证书、nginx 等备份
- 【ZYT keeper】zyt_keeper 库备份（events/users/snapshots/pool_state 等表导出）
- 【ZYT 合约】部署产物冷备：abi、字节码、构造参数、部署交易哈希、部署私钥（离线介质）
- 【ZYT 资金】上线前底池/多签/营销地址的链上余额快照留档

---

## 阶段 2：上线执行（T-0）

> 按序执行，**每步验证通过才进下一步**；执行人操作、复核人确认输出。

| 序 | 步骤 | 验证点 |
|---|---|---|
| T-30min | 上线群集合：过一遍 checklist、确认回滚基准与中止条件 | 任何人可提中止，决策人拍板 |
| 1 | 数据层变更（如有）：备份 → 迁移脚本 → 验证旧版本兼容 | 迁移日志无错误；旧代码可读新表 |
| 2 | 后端服务发布（滚动/蓝绿） | 健康检查 ok；小流量验证后全量 |
| 3 | 链下服务发布【ZYT keeper 重启】 | indexer 同步 ok / ledger rebuild ok / reconcile 对账一致 / monitor 5 规则就绪 |
| 4 | 前端发布：dist 上传 + `?v=` 参数更新 + CDN 刷新 | 页面加载版本号正确（无旧缓存） |
| 5 | DNS/流量切换（如涉及） | 解析生效；新旧流量比例符合预期 |

**执行纪律：**
- 计划外变更一律拒绝（包括"顺手优化"）
- 每步留存操作命令与输出截图
- 【ZYT 合约】**合约部署不可回滚**——部署动作在 T-1 前完成（含验证），T-0 只做应用层；合约出问题走"暂停 → 新合约迁移"预案（见阶段 5）

---

## 阶段 3：上线后验证（T+0 至 T+2h）

- [ ] 核心链路冒烟（生产环境真实走一遍）：
  通用 = 注册/登录/核心功能/支付路径
  【ZYT】= 钱包连接 → USDT 授权 → 入金铸造 → 算力/产出领取 → 分红 → 卖出（滑点档位）→ 转账（10% 税）→ 强制卖出窗口显示
- [ ] 数据一致性三方核对【ZYT】：链上合约状态 = keeper `pool_state` = 前端展示；`reconcile` 对账 diff = 0
- [ ] 错误率与延迟：API 错误率 < 1%、P95 延迟在基线内
- [ ] 第三方依赖验证：RPC 节点、CDN、支付/短信等外部服务
- [ ] 用户反馈通道开启（客服/公告），前 2 小时高频观察
- [ ] 发布公告（如需）：变更内容、已知问题、反馈渠道

---

## 阶段 4：监控要点（上线后 72h 加密巡检，之后转常态）

| 层 | 监控项 | 告警示例 |
|---|---|---|
| 基础设施 | CPU/内存/磁盘/带宽、进程存活（pm2） | 进程退出、磁盘 > 80% |
| 应用层 | 错误日志速率、API 错误率/延迟、慢查询 | 5 分钟错误率 > 5% |
| 业务层 | 关键指标环比突变 | 【ZYT】入金/卖出量异常波动 |
| 链下层 | 【ZYT keeper】R1 底池突变 / R2 大额卖出 / R3 滑点跳变 / R4 索引延迟 / R5 失败率 | 5 规则独立冷却，告警触达实测 |
| 链上层 | 【ZYT】每日快照成功（08:00-08:10 窗口盯守）、对账 diff、合约事件异常、多签操作告警 | 快照未在窗口内完成 → P0 |

- 告警分级：P0 立即电话 / P1 15 分钟响应 / P2 工作时间处理
- 每日固定巡检项【ZYT】：快照是否成功、对账是否一致、keeper 进程存活、备份任务是否执行

---

## 阶段 5：回滚预案

### 5.1 触发条件分级

| 级别 | 判定 | 动作 |
|---|---|---|
| P0 | 核心功能不可用 / 资金或数据安全风险 / 错误率超阈值 | **立即回滚**，决策时限 10 分钟 |
| P1 | 非核心功能异常 / 性能劣化 | 优先 hotfix，必要时降级开关 |
| P2 | 不影响主流程的体验问题 | 记录，随下版本修复 |

### 5.2 分层回滚步骤

- **前端**：恢复上一版 dist + `?v=` 参数回退 + CDN 刷新（分钟级生效）
- **后端**：pm2 切回上一版本（部署目录保留最近 N 版）；前提 = DB schema 向前兼容
- **数据库**：原则**只回代码不回数据**；schema 变更遵循可逆设计（只加列不删列）；极端情况用备份恢复 + 数据补偿脚本
- **配置**：env 回退上一版本并重启
- 【ZYT 合约】不可升级合约**无回滚**——预案链：① 合约 pause 止血（如具备）→ ② 影响评估 → ③ 新合约部署 + 前端/keeper 切换地址 + 资金按链上状态迁移 → ④ 公告与用户沟通
- 【ZYT keeper】回滚 = 恢复上一版本代码 + START_BLOCK 对齐；快照数据（snapshots 表）保留用于对账

### 5.3 回滚后行动

- [ ] 复盘：根因、时间线、责任、改进项（48h 内出报告）
- [ ] hotfix 走完整流程（修复 → 测试 → 验收）后重新上线
- [ ] 回滚演练：上线前至少演练一次前端+后端回滚；此后每季度一次

---

## 附：上线当日时间表模板

| 时间 | 动作 | 负责人 | 复核人 | 验证结果 |
|---|---|---|---|---|
| T-30min | 群集合、checklist 过审 | | | |
| T-20min | 最终备份（DB + 配置） | | | |
| T-10min | 冻结监控基线（当前指标截图） | | | |
| T-0 | 按阶段 2 步骤 1-5 顺序执行 | | | 每步记录 |
| T+30min | 核心链路冒烟 | | | |
| T+1h | 数据一致性核对 | | | |
| T+2h | 上线确认 / 回滚决策点 | | | |
| T+24h | 复盘会 | | | |

> 文档版本：v1.0（2026-09-02）｜ 关联文档：众赢币(ZYT)_技术方案.md §8
